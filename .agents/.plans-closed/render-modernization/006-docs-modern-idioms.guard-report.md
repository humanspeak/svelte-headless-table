# Guard report — 006 docs-modern-idioms

**Recommendation: PASS** — docs no longer point at retired packages, the `fromStore` idiom is shown and proven to render, snippet cells are documented, and `Subscribe` stays discoverable.
**Reviewed at** 50078be · 2026-09-25 15:03 · **Plan planned at** 520f2dd (re-baselined; drift check empty)
**Integrated** — batch complete; PR is the operator's decision (dispatch convention: one branch → one PR, opened on request).

## Done criteria

| Criterion                                                                                   | Result | Evidence                                                            |
| ------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------- |
| `grep -rn "svelte-render\|svelte-subscribe" docs/src/routes README.md` → none               | met    | none                                                                |
| `cd docs && pnpm check` exits 0 and `cd docs && pnpm build` exits 0                         | met    | `0 ERRORS`; `✓ built in 40.86s`                                     |
| `quick-start/Demo.svelte` no longer imports `Subscribe`                                     | met    | 0 occurrences                                                       |
| create-render page documents `createSnippetRender`; render page lists `SnippetRenderConfig` | met    | 3 and 3 occurrences                                                 |
| `git status --porcelain` lists only in-scope files                                          | met    | 7 files; `demo-loaders.ts` churn reverted, no `docs/static` changes |
| README status row for 006 updated                                                           | met    | by guard in this commit                                             |

## Spirit

The plan asked for the docs to tell the truth about the new state — first-party primitives, a native Svelte 5 idiom, the new snippet feature — without stranding users on the legacy template. The diff does that: the quick-start leads with `fromStore` and keeps a "Using Subscribe" section, the API pages drop the "based on" provenance notes, and the README example is the modern shape. The 30+ plugin demos still use `Subscribe`, which is the plan's explicit out-of-scope choice, not a gap.

## Scope & conduct

- In-scope only? yes.
- STOP conditions respected? yes — none fired.
- Plan amendments during execution: pre-flight re-baseline with environment facts (557b8cf).

## Residual risk / follow-ups

- `docs/static/**` LLM mirrors are regenerated by the docs-kit build in CI/deploy; they were not hand-edited here and will pick up the new wording on the next deploy.
- Plugin demos under `docs/src/routes/docs/plugins/**` remain on `Subscribe`; migrate opportunistically using the quick-start demo as the reference.
