# Implementation Plans — view model reuse

> **CLOSED 2026-08-27.** Plan 001 executed by Codex (GPT) and verified by guard:
> **PASS**. Delivered `options.reuseKey` on `Table#createViewModel`, plus 8
> regression tests pinning plugin-state survival across a rebuild. Landed as
> `48847c1` on `chore/touchups`. Nothing remains for the operator inside this
> batch; the branch is unpushed and no PR has been opened.

Generated by the improve skill on 2026-08-27, against commit `29b4ec0`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Context for this initiative

`Table#createViewModel` rebuilds from scratch on every call, and building a view
model instantiates every plugin. A consumer whose column array is derived — new
array identity per reactive pass, same columns inside — therefore discards all
plugin state on each rebuild: pagination returns to page 1, sort keys are lost,
and (before the fix below) the virtual-scroll container was left bound to a dead
closure.

This was scoped as three tracks. **Track A is already done and committed on
branch `chore/touchups`** — `addVirtualScroll`'s state was hoisted into its
config closure so the action identity, scroll position and measured heights
survive a rebuild (`9963373` red tests, `29b4ec0` implementation). Track A does
not generalize: `addSortBy`, `addTableFilter`, `addGroupBy`,
`addResizedColumns`, `addColumnFilters` and `addSelectedRows` genuinely consume
the per-view-model `columnOptions` / `tableState` argument, so their state
cannot move up a level.

**Tracks B and C are what plan 001 covers**, and they collapse into a single
plan rather than two: Track C's remaining item (C3 — asserting `pageIndex`
survives a rebuild) tests an API that Track B introduces, so it cannot be
executed or verified ahead of it. Splitting them would produce a plan whose
done criteria is "these tests fail," which is an invitation for an executor to
implement the other half unprompted. C1 and C2 — the virtual-scroll rebuild and
remount tests — already landed with Track A.

## Execution order & status

| Plan | Title                                                     | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Let `Table#createViewModel` reuse an unchanged view model | P1       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 depends on nothing in this batch. It does assume the Track A fix at
  `29b4ec0` is present, because one of its STOP conditions is unexpected
  failures in `addVirtualScroll.test.ts`.
- Reuse and the Track A hoist are complementary, not redundant. Reuse preserves
  state when the caller can promise the columns are unchanged; the hoist
  preserves the DOM binding even when it can't, because a genuine column change
  still rebuilds and still re-runs no action.

## Findings considered and rejected

- **Automatic structural signature instead of an explicit `reuseKey`.** The
  original sketch was to hash the columns — flat ids plus an accessor/header
  signature — and reuse on a match, which would need no consumer change at all.
  Rejected: headers can be renderer components and accessors are frequently
  inline arrows, so any identity-based signature misses on exactly the derived-
  column-array callers it is meant to serve, and any deeper comparison over
  closures and components is unsound. An explicit key is a smaller win — it
  demotes a consumer-side column-comparison guard to a key expression rather
  than deleting it — but it is honest about who is making the promise.
- **Hoisting state in the remaining stateful plugins, Track-A style.** Rejected,
  but not for the reason first recorded here. The original wording said six
  plugins "consume their per-view-model init argument" and implied that blocked
  hoisting. A later review checked all six and found the distinction that
  wording blurred: `columnOptions` / `tableState` are consumed by each plugin's
  derives and hooks, never by its state stores, which are built from config
  alone (`createSortKeysStore(initialSortKeys)`, `arraySetStore(initialGroupByIds)`,
  and so on). A state-only hoist is therefore feasible for all of them.

    The real reason to prefer reuse: hoisting changes reset semantics for every
    existing consumer, silently and without opt-out — a caller who rebuilds
    deliberately to clear sort or pagination would stop getting a reset with no
    compile error and no runtime signal. `reuseKey` is opt-in, so nobody's
    behavior changes until they ask for it. Hoisting remains available later if a
    plugin's state proves worth preserving unconditionally, and `addVirtualScroll`
    is the case where it was: its breakage is a dead DOM binding, which no
    view-model cache can reach, because a genuine column change still rebuilds and
    Svelte action identity is not reactive.

- **A keyed map instead of a single cache slot.** Rejected: it would pin plugin
  instances for every key ever seen for the life of the `Table`. The cost is
  that alternating between two keys rebuilds every time; that is a silent loss
  of benefit rather than a correctness bug, and it is recorded in 001's
  maintenance notes.

## Guard verification record — plan 001

Executor: Codex (GPT), dispatched via the codex-companion `task --write --fresh`
path at plan baseline `362d2bc`. Verdict: **PASS**. Every criterion below was
reproduced locally by guard, not accepted from the executor's report.

| Criterion                            | Result                                                                                          |
| ------------------------------------ | ----------------------------------------------------------------------------------------------- |
| `pnpm check`                         | `COMPLETED 849 FILES 0 ERRORS 3 WARNINGS` — the 3 warnings are pre-existing, in `src/routes/**` |
| `trunk check` on all 6 changed files | `Checked 6 files ✔ No issues`                                                                   |
| `pnpm test:only`                     | 40 files, 497 tests, all pass                                                                   |
| `pnpm build`                         | ends `All good!`, publint clean                                                                 |
| Scope                                | 6 files changed, all on the plan's in-scope list; no plugin source `.ts` touched                |
| Red-state reproduction               | `src/lib/createTable.ts` reverted to `362d2bc` → exactly the 4 predicted failures, 88 passing   |

The red-state reproduction is the load-bearing check: it confirms the 4 new
assertions fail without the implementation and pass with it, rather than being
tautologies that would have passed either way.

### Deviation accepted

The executor changed `plugins: Plugins = {} as any` to `{} as Plugins` in
`src/lib/createTable.ts`, removing a pre-existing `eslint-disable-next-line`.
This was **forced by a defect in plan 001**: its done criteria included
`grep -rn "eslint-disable" src/lib/createTable.ts` returns no matches, written
as a convention check without noticing the file already carried one on
unrelated code. The executor was right to satisfy the criterion as written. The
change is runtime-identical — `{}` either way — and narrows an `any` to the
declared type parameter, so it was accepted rather than reverted. The
plan-authoring lesson: grep-based convention criteria must be checked against
the current file before they ship in a plan.
